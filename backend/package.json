{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo 'No build step defined'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "aws cloudformation deploy --template-file aws/cloudformation/template.yml --stack-name DevAssistStack --capabilities CAPABILITY_IAM"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1691.0"
  }
}
